{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json,os; inp=json.load(sys.stdin); f=inp.get('file_path',''); exit(0) if not f.endswith('.py') else os.system(f'ruff check --fix \"{f}\" 2>/dev/null')\""
          }
        ]
      }
    ]
  }
}
